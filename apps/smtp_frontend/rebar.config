%{erl_opts, [debug_info, fail_on_warning]}.

{lib_dirs, ["..", "../../deps"]}.

{deps_dir, ["../../deps"]}.

{deps, [
        gen_smtp,
        iconv,
        datatypes,
        game,
        utils,
        controller_app,
        db,
        meck
       ]}.
