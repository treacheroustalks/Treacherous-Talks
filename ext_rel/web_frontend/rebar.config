{sub_dirs, ["rel"]}.

{require_otp_vsn, "R13B04|R14"}.

{cover_enabled, true}.

{erl_opts, [debug_info, fail_on_warning]}.

{deps_dir, ["../../deps"]}.

{deps, [
    nitrogen_core,
	nprocreg,
    simple_bridge,
    sync
]}.
