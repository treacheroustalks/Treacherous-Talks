% -*- mode: erlang -*-

% This is NOT an app, not an OTP release!
% Rebar is only used for compiling the dependencies.

{lib_dirs, ["../../deps",
            "../../apps"]}.

{deps_dir, ["deps"]}.

{deps, [
    datatypes,
    command_parser,
    controller_app,
    game,
    service
]}.
