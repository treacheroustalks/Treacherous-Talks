%%-*- mode: erlang;-*-
{sys, [
       {lib_dirs, []},
       {rel, "xmpp_frontend", "1",
        [
         kernel,
         stdlib,
         sasl
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "xmpp_frontend"},
       {profile, embedded},
       {excl_archive_filters, [".*"]}, %% Do not archive built libs
       {excl_sys_filters, ["^bin/.*",
                           "^erts.*/bin/(dialyzer|typer)"]},

       %% Including HiPE can cause issues generating your first upgrade.
       %% If you plan to distribute HiPE in your release remove the
       %% following line.
       {app, hipe, [{incl_cond, exclude}]},

       {app, sasl, [{incl_cond, include}]}
      ]}.

{target_dir, "xmpp_frontend"}.

{overlay, [
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {copy, "files/xmpp_frontend", "bin/xmpp_frontend"},
           {copy, "files/sys.config", "releases/\{\{rel_vsn\}\}/sys.config"},
           {copy, "files/xmpp_frontend.cmd", "bin/xmpp_frontend.cmd"},
           {copy, "files/start_erl.cmd", "bin/start_erl.cmd"},
           {copy, "files/vm.args", "releases/\{\{rel_vsn\}\}/vm.args"},
           {copy, "../ejabberd-installation", "ejabberd-installation"},
           {copy, "../update-installation", "update-installation"},
           {copy, "../build_erts", "build_erts"},
           {copy, "../installation_prefix", "installation_prefix"}
          ]}.
