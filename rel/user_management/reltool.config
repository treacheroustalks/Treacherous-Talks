{sys, [
       {lib_dirs, ["../../apps",
                   "../../deps"]},
       {rel, "user_management", "1",
        [
         kernel,
         stdlib,
         sasl,
         datatypes,
         service,
         db,
         user_management
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "user_management"},
       {profile, embedded},
       {excl_archive_filters, [".*"]}, %% Do not archive built libs
       {excl_sys_filters, ["^bin/.*",
                           "^erts.*/bin/(dialyzer|typer)"]},

       %% Including HiPE can cause issues generating your first upgrade.
       %% If you plan to distribute HiPE in your release remove the
       %% following line.
       {app, hipe, [{incl_cond, exclude}]},

       {app, sasl, [{incl_cond, include}]},
       % Exclude exmpp since reltool will give a warning about ejabberd_socket
       % being included by two different applications
       {app, exmpp, [{incl_cond, exclude}]}
      ]}.

{target_dir, "user_management"}.

{overlay, [
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {copy, "files/user_management", "bin/user_management"},
           {copy, "files/sys.config", "etc/app.config"},
           {copy, "files/user_management.cmd", "bin/user_management.cmd"},
           {copy, "files/start_erl.cmd", "bin/start_erl.cmd"},
           {copy, "files/vm.args", "etc/vm.args"}
          ]}.
